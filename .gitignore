# Runtime logs and pid files (generated by start.ps1)
logs/

# PocketBase binary and archives — download separately
backend/pocketbase.exe
backend/*.zip

# PocketBase runtime data — keep pb_migrations, not db files
backend/pb_data/*.db
backend/pb_data/*.db-shm
backend/pb_data/*.db-wal
backend/pb_data/types.d.ts

# Frontend build output
frontend/.svelte-kit/
frontend/build/
frontend/node_modules/

# Environment files with secrets
frontend/.env
# (keep .env.example in git)

# Python scratch / dev files
__pycache__/
*.pyc
.venv/
label_area.png
portrait_rows_labeled.png

# Editor / tool state
.claude/

# OS
.DS_Store
Thumbs.db
